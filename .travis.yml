language: java
install: true

jdk:
  - oraclejdk8


addons:
  firefox: "61.0"

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
  - ./gradlew build --continue